#include<stdio.h>
int main()
{
int x;
printf("enter the number");
scanf("%d",&x);
if (x%2==0)
{
    printf( " x is an even number ");
    }
else {
printf( " x is not an even number");
}

return 0;
}